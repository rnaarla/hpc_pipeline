[pytest]
addopts = -q
testpaths =
    tests
python_files = test_*.py
markers =
    unit: Fast-running unit tests.
    gpu: Tests that require GPU hardware.
    integration: Integration tests requiring full pipeline.
    production: Production readiness validation suite.
    stress: Stress tests that may take longer or require special hardware.
